name: Python-linter

# Specify the situation more specifically
on:
  push:
    branches:
      - main

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello Message
        run : echo "Hello World!"
          
          
          
          
  second_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Good morning
        run : echo "Good morning"
    
    
